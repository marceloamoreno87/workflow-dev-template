# Separate client and portfolio GitHub Projects

Client-facing Work Items appear in a GitHub Project whose access boundary matches that client, while the operator maintains a separate private organization-level portfolio Project. A Work Item may be projected into both views, but client membership must never grant visibility into another client's planning or repository context.
