# Version structured artifacts

Every persisted manifest, registry, Event payload, Agent Role envelope, Spec schema, and configuration document carries an explicit schema version. Readers support bounded compatibility, migrations are previewable and backed up, projection state remains rebuildable, and automatic binary updates or database downgrades are prohibited.
