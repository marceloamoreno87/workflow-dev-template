# Provide a CLI and a loopback dashboard

The Harness exposes an automatable CLI and a local web dashboard served by the daemon on loopback only. GitHub Projects remain the client-facing interface; the dashboard is the operator surface for queues, traces, costs, Human Gates, environment leases, and diagnostics.
