# Implement the Harness in Go

The daemon and CLI are implemented in Go and distributed as a single versioned binary. Go provides a compact operational footprint and explicit primitives for process supervision, concurrency, cancellation, and local service operation; Project stacks remain independent behind Stack Adapters.
